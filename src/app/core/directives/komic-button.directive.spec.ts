import { KomicButtonDirective } from './komic-button.directive';

describe('KomicButtonDirective', () => {
  it('should create an instance', () => {
    const directive = new KomicButtonDirective();
    expect(directive).toBeTruthy();
  });
});
