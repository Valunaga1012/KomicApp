import { KomicCardSmallDirective } from './komic-card-small.directive';

describe('KomicCardSmallDirective', () => {
  it('should create an instance', () => {
    const directive = new KomicCardSmallDirective();
    expect(directive).toBeTruthy();
  });
});
