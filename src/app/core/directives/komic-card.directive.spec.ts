import { KomicCardDirective } from './komic-card.directive';

describe('KomicCardDirective', () => {
  it('should create an instance', () => {
    const directive = new KomicCardDirective();
    expect(directive).toBeTruthy();
  });
});
