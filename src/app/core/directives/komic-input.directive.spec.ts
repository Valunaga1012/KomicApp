import { KomicInputDirective } from './komic-input.directive';

describe('KomicInputDirective', () => {
  it('should create an instance', () => {
    const directive = new KomicInputDirective();
    expect(directive).toBeTruthy();
  });
});
